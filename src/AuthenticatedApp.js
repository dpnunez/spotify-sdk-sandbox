import React from "react";
import { Route, Redirect, Switch } from "react-router-dom";
import { Login } from "./pages";

const AuthenticatedApp = () => <Switch></Switch>;

export default AuthenticatedApp;
